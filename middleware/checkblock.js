
const isblocked =(req,res,next) => {
    try {
        const userId = req.user._id;
        const id = req.params.id;
        

    } catch (error) {
        
    }
};

export default isblocked;